# Klavaranto — Python Dependencies
#
# Install all dependencies by running this command in your terminal:
#   pip install -r requirements.txt
#
# On Linux, you may also need to install system packages
# for pynput to work properly. See the README for details.

pynput    # Global keyboard listener
pystray   # System tray icon
Pillow    # Icon and flag generation